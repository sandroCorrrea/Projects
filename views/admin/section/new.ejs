<%- include('../../../partials/head.ejs') %>
<link rel="stylesheet" type="text/css" href="/css/newStyle.css">
<%- include('../../../partials/head2.ejs') %>
<body>
    <%- include('../../../partials/navbar.ejs') %>

    <div class="container">
        <div class="card">
            <div class="card-header">
                <h3 class="display-4">Seções de Criação</h3>
            </div>
            <div class="card-body">
                <form method="POST" action="/admin/section/save">
                    <br><label>Artigos</label>
                    <select class="form-control" name="articleSelect">
                        <% articles.forEach(article => { %>
                            <option value="<%=article.id%>"><%= article.title %></option>
                        <% }); %> 
                    </select>
                    <br><label>Título</label>
                    <input type="text" name="title" class="form-control">
                    <br><textarea class="form-control" id="section" name="corpo">
                    </textarea>
                    <br><button class="btn btn-success">Cadastrar</button>
                </form>
            </div>
        </div>
        <hr>
    </div>
</body>
<%- include('../../../partials/footer.ejs') %>

<script src="/tinymce/tinymce.min.js"></script>
<script>
    tinymce.init({
        selector: '#section',
        language: 'pt_BR',
        plugins: [
            'advlist autolink hr lists table print insertdatetime preview emoticons image bbcode code autosave',
        ]
    })
</script>